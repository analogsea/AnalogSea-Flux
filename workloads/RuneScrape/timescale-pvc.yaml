kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: timescale-pvc
  labels:
    app: timescale
  namespace: runescrape
spec:
  storageClassName: csi-rbd
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 70Gi
