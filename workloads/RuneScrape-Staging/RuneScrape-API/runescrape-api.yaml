---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: runescrape-api
  labels:
    app: runescrape-api
  annotations:
    fluxcd.io/automated: "true"
    fluxcd.io/tag.runescrape-api: glob:staging-continuous-*
    fluxcd.io/tag.initdb: glob:staging-continuous-*
  namespace: runescrape-staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: runescrape-api
  template:
    metadata:
      labels:
        app: runescrape-api
    spec:
      initContainers:
      - name: initdb
        image: iexalt/runescrape-api:staging-continuous-51
        command: ['flask', 'db', 'upgrade']
        env:
        - name: DB_HOST
          value: "timescale.runescrape-staging"
        - name: DB_USER
          value: "postgres"
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: timescale-pass
              key: pass

      containers:
      - name: runescrape-api
        image: iexalt/runescrape-api:staging-continuous-51
        ports:
        - containerPort: 8080
        env:
        - name: DB_HOST
          value: "timescale.runescrape-staging"
        - name: DB_USER
          value: "postgres"
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: timescale-pass
              key: pass
